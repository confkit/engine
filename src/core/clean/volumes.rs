use anyhow::Result;

struct VolumesCleaner;

impl VolumesCleaner {
    pub async fn clean_workspace(all: bool) -> Result<()> {
        Ok(())
    }

    pub async fn clean_artifacts(all: bool) -> Result<()> {
        Ok(())
    }
}
