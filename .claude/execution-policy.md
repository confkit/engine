# 执行策略

## 禁止操作 (未经明确许可)

- **开发服务器**: 不要启动开发服务器、监听器或后台进程
- **构建命令**: 不要执行构建、编译或打包操作
- **Git 变更**: 不要创建提交、推送变更或修改 git 历史
- 允许 Git 查询操作 (status、log、diff)
- **测试执行**: 不要运行测试套件或测试命令

## 需要确认

执行以下操作前必须明确询问:

- 安装或更新依赖
- 执行任何资源密集型操作
- 修改项目配置文件
- 执行部署相关命令

## 网络请求

访问外网资源时使用代理:

```bash
export https_proxy=http://127.0.0.1:7890 http_proxy=http://127.0.0.1:7890 all_proxy=socks5://127.0.0.1:7890
```
