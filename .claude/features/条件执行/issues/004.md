---
name: Interactive 环境变量累积机制
feature: 条件执行
status: 未开始
progress: 0
dependencies: []
---

# Interactive 环境变量累积机制

## 议题目标
实现交互式环境变量的条件执行和累积传递，支持前面选择影响后续选项的渐进式决策

## 实现要点
- 修改 process_interactive_environments 函数支持条件字段
- 实现环境变量的渐进式累积机制
- 在交互过程中动态评估每个交互项的条件
- 确保条件评估失败时的优雅降级
- 支持条件不满足时跳过交互项

## 技术细节
在 core/interactive/run.rs 中修改 process_interactive_environments 函数：

1. 核心累积逻辑实现：
   ```rust
   pub async fn process_interactive_environments(
       configs: &[ConfKitEnvironmentInteractiveConfig],
       initial_env: HashMap<String, String>,
   ) -> Result<HashMap<String, String>> {
       let mut accumulated_env = initial_env;
       
       for config in configs {
           // 条件评估
           if let Some(condition) = &config.condition {
               let evaluator = ConditionEvaluator::new(accumulated_env.clone());
               match evaluator.evaluate(condition) {
                   Ok(true) => {}, // 条件满足，继续交互
                   Ok(false) => {
                       println\!(\

## Todo 列表
[ ] 分析现有 process_interactive_environments 函数实现
[ ] 导入 ConditionEvaluator 到 interactive 模块
[ ] 实现环境变量累积的核心逻辑
[ ] 在交互循环中添加条件评估
[ ] 实现条件不满足时的跳过逻辑
[ ] 实现条件评估失败的错误处理
[ ] 添加用户友好的跳过提示信息
[ ] 编写交互式条件执行的测试用例
